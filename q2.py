# Create a program that calculate the area of a rectangular . Take the length and width as inputs from the user and store them in variables. Calcualte and display the area..

length = float(input("Enter the length of ractangular: "))
width = float(input("Enter the width of ractangualr: "))

area_of_rectangular= length * width

print("The area of rectangular is : " , area_of_rectangular)